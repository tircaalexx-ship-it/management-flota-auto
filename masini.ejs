<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titlu %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">🚗 Management Flotă Auto</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/">Panou Control</a>
                <a class="nav-link active" href="/masini">Mașini</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>Gestionare Mașini</h2>
            <a href="/masini/adauga" class="btn btn-success">
                <i class="fas fa-plus"></i> Adaugă Mașină Nouă
            </a>
        </div>

        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Marcă</th>
                        <th>Model</th>
                        <th>An</th>
                        <th>Nr. Înmatriculare</th>
                        <th>Consum (l/100km)</th>
                        <th>Stare</th>
                        <th>Acțiuni</th>
                        <td>
    <div class="btn-group btn-group-sm">
        <% if (permisiuni && permisiuni.includes('editare')) { %>
            <a href="/masini/editeaza/<%= masina.id %>" class="btn btn-primary">
                <i class="fas fa-edit"></i> Editează
            </a>
        <% } %>
        
        <% if (permisiuni && permisiuni.includes('stergere')) { %>
            <form action="/masini/sterge/<%= masina.id %>" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-danger" onclick="return confirm('Sigur vrei să ștergi această mașină?')">
                    <i class="fas fa-trash"></i> Șterge
                </button>
            </form>
        <% } %>
    </div>
</td>
                    </tr>
                </thead>
                <tbody>
                    <% masini.forEach(masina => { %>
                    <tr>
                        <td><%= masina.id %></td>
                        <td><%= masina.marca %></td>
                        <td><%= masina.model %></td>
                        <td><%= masina.an %></td>
                        <td><strong><%= masina.nrInmatriculare %></strong></td>
                        <td><%= masina.consum %></td>
                        <td>
                            <span class="badge 
                                <% if (masina.stare === 'Activă') { %>bg-success<% } %>
                                <% if (masina.stare === 'În service') { %>bg-warning<% } %>
                                <% if (masina.stare === 'Închiriată') { %>bg-info<% } %>
                            "><%= masina.stare %></span>
                        </td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <a href="/masini/editeaza/<%= masina.id %>" class="btn btn-primary">
                                    <i class="fas fa-edit"></i> Editează
                                </a>
                                <form action="/masini/sterge/<%= masina.id %>" method="POST" style="display: inline;">
                                    <button type="submit" class="btn btn-danger" onclick="return confirm('Sigur vrei să ștergi această mașină?')">
                                        <i class="fas fa-trash"></i> Șterge
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <% if (masini.length === 0) { %>
        <div class="alert alert-info text-center">
            <h4>Nu există mașini în flotă</h4>
            <p>Începe prin a adăuga prima mașină.</p>
            <a href="/masini/adauga" class="btn btn-success">Adaugă Prima Mașină</a>
        </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>